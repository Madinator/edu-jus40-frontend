import { NoopValueAccessorDirectiveDirective } from './noop-value-accessor-directive.directive';

describe('NoopValueAccessorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NoopValueAccessorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
